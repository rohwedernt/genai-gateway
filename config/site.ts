export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "NRDC GenAI Gateway",
  description: "Compares Chatbot responses for variable models, rate limiting and recommends an answer based on confidence level.",
  links: {
    github: "https://github.com/rohwedernt/genai-gateway",
  },
};
